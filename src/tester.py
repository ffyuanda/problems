# import re
#
# pattern = "^([a-z]+)(_prev)([0-9]*?)$"
# m = re.match(pattern, "sasda_prev")
# print(len(m.group(3)))

print(['2'] * 3)

from collections import defaultdict

i = defaultdict(int)
i['x'] = 1
# i.pop('x')
print(i)